# CPU-only stack: dataset validation, splitting, leakage detection, tests, CI.
# Deliberately excludes torch / transformers / bitsandbytes.

numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
matplotlib>=3.9.0
PyYAML>=6.0.1

fastapi>=0.115.0
pydantic>=2.9.0

httpx>=0.27.0          # required by fastapi.testclient
pytest>=8.3.0
pytest-cov>=6.0.0
ruff>=0.8.0
locust>=2.32.0
