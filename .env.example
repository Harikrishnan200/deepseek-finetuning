# Hugging Face access token (write scope) used to push the LoRA adapter.
# Create one at https://huggingface.co/settings/tokens
HF_TOKEN=

# Optional: default repo id used by scripts/train_qlora.py --push-to-hub
HF_HUB_MODEL_ID=
